{
	"name": "hello_world",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "cross-env NODE_ENV=production node index.js",
		"dev": "cross-env NODE_ENV=development nodemon index.js",
		"build:ui": "rm -rf build && cd /home/mhucanada/Projects/FullStackOpen/notes_project/notes-new && npm run build --prod && cp -r build /home/mhucanada/Projects/FullStackOpen/notes_project/notes-backend",
		"deploy": "git push heroku master",
		"deploy:full": "npm run build:ui && git add . && git commit -m uibuild && npm run deploy",
		"logs:prod": "heroku logs --tail",
		"lint": "eslint .",
		"test": "cross-env NODE_ENV=test jest --verbose --runInBand"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"cross-env": "^7.0.2",
		"dotenv": "^8.2.0",
		"express": "^4.17.1",
		"express-async-errors": "^3.1.1",
		"mongoose": "^5.10.0"
	},
	"devDependencies": {
		"eslint": "^7.7.0",
		"jest": "^26.4.1",
		"nodemon": "^2.0.4",
		"supertest": "^4.0.2"
	},
	"jest": {
		"testEnvironment": "node"
	}
}
